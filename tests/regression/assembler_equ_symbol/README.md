# assembler_equ_symbol

Purpose: verifies that an `.equ` expression based on a symbol plus a literal is calculated in the symbol table.

Expected result: assembler succeeds and `start_plus_four` has value `4` in the textual symbol table.
