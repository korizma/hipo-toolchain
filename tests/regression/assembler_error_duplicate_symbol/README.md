# assembler_error_duplicate_symbol

Purpose: verifies that defining the same symbol label twice is reported as an assembler error.

Expected result: assembler exits with failure and prints `Double symbol declaration: same`.
